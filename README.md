## Vanilla Transformer as described in "attention is all you need"

This is a personal project. I'm reusing the original implementation of the "attention is all you need", but removing all dependencies on pytorch NLP helpers and pytorch datasets.  Also updating the original code to work with torch 1.11+ new features.
